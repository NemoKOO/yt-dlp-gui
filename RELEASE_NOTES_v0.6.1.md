# v0.6.1 — 连续下载操作修正

- 修正连续下载不同视频时，输入框仍可能读取上一条网址的问题。
- 当输入框中存在多个网址时，改为使用最后粘贴的网址。
- 网址输入框改为“粘贴即替换”：`Ctrl+V`、右键粘贴和 `Shift+Insert` 都会用新内容直接替换旧内容，不需要手动清空。
- 保留 v0.6 的视频分辨率选择：360p、480p、720p、1080p、1440p（2K）和 2160p（4K）。

建议使用 Windows 10/11 x64。Windows 7 不支持当前 yt-dlp 官方独立程序。
